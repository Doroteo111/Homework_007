using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EXERCISE_5 : MonoBehaviour
{
    // Make a sphere, by default blue, change its position to a random position  every 2 seconds with a corrutine.
    // If we hit the sphere before it disappears, then the color of the sphere will change to green and we earn a
    // point.Once we hit it, we cannot hit it again. After that 2 seconds, the sphere must appear in a random
    // position and blue again. If the sphere disappears before we hit it, it should sound a sound of defeat.
    // We can only miss 3 times.Once we fail 3 times, Game Over. You must show the points and the lives with
    // Game Objects TextMeshPro.

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
